{
  "name": "scriptsxo-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute scriptsxo --remote --file=../schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250307.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.0.0"
  }
}
